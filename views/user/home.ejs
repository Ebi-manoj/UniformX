<section class="relative bg-gray-50 px-4 sm:px-6 lg:px-36">
      <div class="mx-auto">
        <div class="grid grid-cols-1 md:grid-cols-2 gap-10 items-center">
          <div>
            <h1 class="text-5xl font-medium text-gray-900">
              Fresh Arrivals Online
            </h1>
            <p class="mt-2 text-base text-gray-600">
              Discover Our Newest Collection Today
            </p>
            <a href="/products">
              <button
              class="mt-6 bg-gray-900 text-white px-6 py-2 text-sm font-medium rounded hover:bg-gray-800 transition-colors"
            >
              SHOP COLLECTION
            </button>
            </a>
          </div>
          <div class="flex justify-end">
            <div class="relative size-[450px] rounded-full overflow-hidden">
              <div
                class="absolute bottom-0 right-10 size-96 bg-gray-200 p-10 rounded-full"
              >
                <img
                  src="images/messi.png"
                  alt="Model in white outfit"
                  class="absolute right-3 -top-14 h-[600px] grayscale-[0.6] brightness-150 saturate-50"
                />
              </div>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- Features Section -->
    <section class="py-12 px-4 sm:px-6 lg:px-8 bg-white">
      <div class="max-w-7xl mx-auto">
        <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
          <!-- Free Shipping -->
          <div class="flex flex-col items-center text-center">
            <div
              class="w-12 h-12 flex items-center justify-center rounded-full bg-gray-100 mb-4"
            >
              <i class="fas fa-truck text-gray-600"></i>
            </div>
            <h3 class="font-medium text-gray-900">Free Shipping</h3>
            <p class="mt-2 text-sm text-gray-500">
              On orders over $50. Delivery time: 3-5 business days.
            </p>
          </div>

          <!-- Satisfaction Guarantee -->
          <div class="flex flex-col items-center text-center">
            <div
              class="w-12 h-12 flex items-center justify-center rounded-full bg-gray-100 mb-4"
            >
              <i class="fas fa-shield-alt text-gray-600"></i>
            </div>
            <h3 class="font-medium text-gray-900">Satisfaction Guarantee</h3>
            <p class="mt-2 text-sm text-gray-500">
              Shop with confidence with our 30-day return policy.
            </p>
          </div>

          <!-- Secure Payment -->
          <div class="flex flex-col items-center text-center">
            <div
              class="w-12 h-12 flex items-center justify-center rounded-full bg-gray-100 mb-4"
            >
              <i class="fas fa-lock text-gray-600"></i>
            </div>
            <h3 class="font-medium text-gray-900">Secure Payment</h3>
            <p class="mt-2 text-sm text-gray-500">
              Pay safely with our secure checkout options.
            </p>
          </div>
        </div>
      </div>
    </section>

    <!-- Best Selling Section -->
    <section class="py-16 px-4 sm:px-6 lg:px-8 bg-white">
      <div class="max-w-7xl mx-auto">
        <div class="text-center mb-12">
          <h2 class="text-2xl font-bold text-gray-900">Best Selling</h2>
        </div>
    
        <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-8">
          <% bestSelling.forEach(product => { %>
            <div class="group border border-gray-100 py-3 px-3 bg-white  rounded-lg overflow-hidden">
              <div class="aspect-w-1 aspect-h-1 bg-white rounded-lg overflow-hidden">
                <a href="/product/<%= product.slug %>">
                  <img 
                  src="<%= product.image_url[0] %>" 
                  alt="<%= product.title %>" 
                  class="w-full h-64 object-contain"
                />
                </a>
              </div>
              <div class="mt-4">
                <h3 class="text-base text-gray-700 font-semibold"><%= product.title %></h3>
                <div class="flex items-center mt-2">
                  <% if (product.discountPercentage) { %>
                    <span class="text-xs text-red-500 line-through mr-2">&#8377;<%= product.price %></span>
                    <span class="text-sm font-bold text-gray-900 mr-2">
                      &#8377;<%= (product.price - (product.price * product.discountPercentage / 100)).toFixed(2) %>
                    </span>
                    <span class="text-sm font-semibold text-green-600">(-<%= product.discountPercentage %>%)</span>
                  <% } else { %>
                    <span class="text-sm font-bold text-gray-900">&#8377;<%= product.price %></span>
                  <% } %>
                </div>
                <div class="mt-2 flex items-center">
                  <div class="flex">
                    <% for(let i = 0; i < 5; i++) { %>
                      <% if (i < Math.round(product.averageRating)) { %>
                        <i class="fas fa-star text-yellow-400 text-xs"></i>
                      <% } else { %>
                        <i class="far fa-star text-gray-300 text-xs"></i>
                      <% } %>
                    <% } %>
                  </div>
                  <span class="ml-2 text-xs text-gray-600">(<%= product.averageRating %>)</span>
                </div>
              </div>
            </div>
          <% }) %>
        </div>
        
      </div>
    </section>
    

    <!-- Game Day Ready Section -->
    <section class=" px-4 sm:px-6 lg:px-8 bg-gray-50">
      <div class="max-w-7xl mx-auto">
        <div class="grid grid-cols-1 md:grid-cols-2 gap-8 items-center">
          <div>
            <h2 class="text-4xl font-bold text-gray-900">
              Game Day Ready - Shop Your Favorite Jerseys!
            </h2>
            <p class="mt-4 text-sm text-gray-600">
              We've got the world's sports merchandise! An exclusive collection
              of jerseys, t-shirts, and apparel collections.
            </p>
            <a href="/products">
              <button
              class="mt-6 bg-gray-900 text-white px-6 py-2 text-sm font-medium rounded hover:bg-gray-800 transition-colors flex items-center"
            >
              EXPLORE MORE
              <svg
                xmlns="http://www.w3.org/2000/svg"
                class="h-4 w-4 ml-2"
                fill="none"
                viewBox="0 0 24 24"
                stroke="currentColor"
              >
                <path
                  stroke-linecap="round"
                  stroke-linejoin="round"
                  stroke-width="2"
                  d="M9 5l7 7-7 7"
                />
              </svg>
            </button>
            </a>
          </div>
          <div class="flex justify-end">
            <img
              src="/images/category_img.png"
              alt="Basketball players"
              class="right-3  h-[400px] grayscale-[0.6] brightness-150 saturate-25"
            />
          </div>
        </div>
      </div>
    </section>

    <!-- More Products Section -->
    <section class="py-16 px-4 sm:px-6 lg:px-8 bg-white">
      <div class="max-w-7xl mx-auto">
        <div class="text-center mb-12">
          <h2 class="text-2xl font-bold text-gray-900">New Features</h2>
        </div>
        <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-8">
          <% features.forEach(product => { %>
            <div class="group border border-gray-100 py-3 px-3 bg-white  rounded-lg overflow-hidden">
              <div class="aspect-w-1 aspect-h-1 bg-white rounded-lg overflow-hidden">
               <a href="/product/<%= product.slug %>">
                <img 
                src="<%= product.image_url[0] %>" 
                alt="<%= product.title %>" 
                class="w-full h-64 object-contain"
              />
               </a>
              </div>
              <div class="mt-4">
                <h3 class="text-base text-gray-700 font-semibold"><%= product.title %></h3>
                <div class="flex items-center mt-2">
                  <% if (product.discountPercentage) { %>
                    <span class="text-xs text-red-500 line-through mr-2">&#8377;<%= product.price %></span>
                    <span class="text-sm font-bold text-gray-900 mr-2">
                      &#8377;<%= (product.price - (product.price * product.discountPercentage / 100)).toFixed(2) %>
                    </span>
                    <span class="text-sm font-semibold text-green-600">(-<%= product.discountPercentage %>%)</span>
                  <% } else { %>
                    <span class="text-sm font-bold text-gray-900">&#8377;<%= product.price %></span>
                  <% } %>
                </div>
                <div class="mt-2 flex items-center">
                  <div class="flex">
                    <% for(let i = 0; i < 5; i++) { %>
                      <% if (i < Math.round(product.averageRating)) { %>
                        <i class="fas fa-star text-yellow-400 text-xs"></i>
                      <% } else { %>
                        <i class="far fa-star text-gray-300 text-xs"></i>
                      <% } %>
                    <% } %>
                  </div>
                  <span class="ml-2 text-xs text-gray-600">(<%= product.averageRating %>)</span>
                </div>
              </div>
            </div>
          <% }) %>
        </div>
      </div>
      <!-- referal icon -->
     <% if (!referalClaimed && isAuthenticated) { %>
      <div class="fixed bottom-4 left-4 z-50">
        <div id="referalBtn" class="inline-flex items-center border text-xs font-semibold focus:outline-none focus:ring-2 focus:ring-ring focus:ring-offset-2 border-transparent text-primary-foreground hover:bg-primary/80 cursor-pointer hover:scale-105 transition-transform bg-white/80 backdrop-blur-sm rounded-full p-2 shadow-lg hover:shadow-xl">
          <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="lucide lucide-gift w-6 h-6 text-primary"><rect x="3" y="8" width="18" height="4" rx="1"></rect><path d="M12 8v13"></path><path d="M19 12v7a2 2 0 0 1-2 2H7a2 2 0 0 1-2-2v-7"></path><path d="M7.5 8a2.5 2.5 0 0 1 0-5A4.8 8 0 0 1 12 8a4.8 8 0 0 1 4.5-5 2.5 2.5 0 0 1 0 5"></path></svg>
        </div>
      </div>
      
     <% } %>
    </section>

    <!-- Newsletter Section -->
    <section class="py-16 px-4 sm:px-6 lg:px-8 bg-gray-50">
      <div class="max-w-7xl mx-auto">
        <div class="grid grid-cols-1 md:grid-cols-2 gap-8 items-center">
          <div>
            <h2 class="text-2xl font-bold text-gray-900">
              Join Our Newsletter
            </h2>
            <p class="mt-2 text-sm text-gray-600">
              Sign up to get the latest on sales, new releases and more...
            </p>
          </div>
          <div>
            <form class="flex flex-col sm:flex-row gap-3">
              <input
                type="email"
                placeholder="Your email address"
                class="flex-1 px-4 py-2 border border-gray-300 rounded focus:outline-none focus:ring-2 focus:ring-gray-900 focus:border-transparent"
              />
              <button
                type="submit"
                class="bg-gray-900 text-white px-6 py-2 text-sm font-medium rounded hover:bg-gray-800 transition-colors"
              >
                SUBSCRIBE
              </button>
            </form>
          </div>
        </div>
      </div>
    </section>

    <!-- referal Modal -->
    <div
    id="referalModal"
    class="fixed inset-0 flex items-center justify-center bg-black bg-opacity-50 z-50 hidden"
  >
    <div
      class="bg-white rounded-xl shadow-lg w-full max-w-3xl flex overflow-hidden"
    >
      <!-- Image Side -->
      <div class="w-1/2 hidden md:flex  justify-center items-center">
        <img
          src="/images/logo.png"
          alt="Skincare model"
          class="max-w-full h-auto object-cover"
        />
      </div>

      <!-- Text Content Side -->
      <div class="w-full md:w-1/2 p-8 relative">
        <!-- Close Button -->
        <button  class="close-btn absolute top-4 right-4 text-black text-xl font-bold">
          &times;
        </button>

        <h2 class="text-lg font-semibold text-gray-700 mb-2">Welcome!</h2>
        <h1 class="text-2xl font-bold text-brown-700 mb-2">
          Got a referral code?
        </h1>
        <p class="text-gray-500 mb-6">
          Enter your code below to receive a bonus in your wallet
        </p>

        <input
          id="referalInput"
          type="text"
          placeholder="Enter your referral code"
          class="w-full px-4 py-2 border border-gray-300 rounded-md mb-4 focus:outline-none focus:ring-2 focus:ring-gray-900"
        />

        <button
          id="applyBtn"
          class="w-full bg-black text-white py-2 rounded-md hover:bg-gray-800 transition mb-3"
        >
          Apply Code
        </button>

        <button
          class="close-btn w-full border border-black text-black py-2 rounded-md hover:bg-gray-100 transition"
        >
          I don't have a code
        </button>
      </div>
    </div>
  </div>
  